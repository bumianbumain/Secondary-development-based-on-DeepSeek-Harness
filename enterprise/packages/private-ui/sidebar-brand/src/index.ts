/** Host loader entry for the enterprise sidebar-brand bundle (browser half in `./client`). */

/** Host plugin body — no host-side behavior; the UI half lives in `./client`. */
export function apply(): void {}
