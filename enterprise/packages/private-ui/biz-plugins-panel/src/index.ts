/** Host loader entry for the enterprise business-plugins panel (browser half in `./client`). */

/** Host plugin body — no host-side behavior; the UI half lives in `./client`. */
export function apply(): void {}
